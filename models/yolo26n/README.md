# YOLO26n model directory

Do not commit large weights directly. Store exported `.onnx`, `.param`/`.bin`, `metadata.json`, and `classes.txt` here through your artifact release process or Git LFS. Record the model source, dataset/license, exporter version, input size, input/output blob names, class order, quantization, and a SHA-256 checksum in release metadata.

